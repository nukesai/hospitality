interface:
  display_name: "Hospitality"
  short_description: "Repo-specific patterns and workflows for hospitality"
  default_prompt: "Use the hospitality repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true